import { ListSecondaryDirective } from './list-secondary.directive';

describe('ListSecondaryDirective', () => {
  it('should create an instance', () => {
    const directive = new ListSecondaryDirective();
    expect(directive).toBeTruthy();
  });
});
