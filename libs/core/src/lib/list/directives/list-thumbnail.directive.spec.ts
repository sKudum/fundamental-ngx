import { ListThumbnailDirective } from './list-thumbnail.directive';

describe('ListThumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new ListThumbnailDirective();
    expect(directive).toBeTruthy();
  });
});
