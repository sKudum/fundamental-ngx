import { ListTitleDirective } from './list-title.directive';

describe('ListTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ListTitleDirective();
    expect(directive).toBeTruthy();
  });
});
