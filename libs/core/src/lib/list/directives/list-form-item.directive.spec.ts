import { ListFormItemDirective } from './list-form-item.directive';

describe('ListFormItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ListFormItemDirective();
    expect(directive).toBeTruthy();
  });
});
