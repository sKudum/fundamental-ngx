import { ListFooterDirective } from './list-footer.directive';

describe('ListFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new ListFooterDirective();
    expect(directive).toBeTruthy();
  });
});
