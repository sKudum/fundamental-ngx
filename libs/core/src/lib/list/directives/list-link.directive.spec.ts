import { ListLinkDirective } from './list-link.directive';

describe('ListLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ListLinkDirective();
    expect(directive).toBeTruthy();
  });
});
