import { ListGroupHeaderDirective } from './list-group-header.directive';

describe('ListGroupHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ListGroupHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
