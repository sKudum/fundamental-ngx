import { ListIconDirective } from './list-icon.directive';

describe('ListIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ListIconDirective();
    expect(directive).toBeTruthy();
  });
});
