import { ListContentDirective } from './list-content.directive';

describe('ListContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ListContentDirective();
    expect(directive).toBeTruthy();
  });
});
