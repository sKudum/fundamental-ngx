import { ListBylineDirective } from './list-byline.directive';

describe('ListBylineDirective', () => {
  it('should create an instance', () => {
    const directive = new ListBylineDirective();
    expect(directive).toBeTruthy();
  });
});
